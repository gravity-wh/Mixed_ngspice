## 交付物
.github/workflows/test.yml 扩展 + compare_fp.py 加 parse_fs_meas()。

## 实现路径
1. test.yml float-spice job:
   每phase一个step: batch_test.sh --phase p51|p52|p53|p56
   p52/p53/p56加 continue-on-error: true (初期容忍)
   回归step: batch_test.sh --regression (必须通过)

2. compare_fp.py parse_fs_meas():
   解析 "meas_name = value" 行
   集成到跨格式比较流程

3. 生成FP64基线:
   运行ngspice-46 → 保存参考log → gen_baseline.py更新

## 测试标准
- CI float-spice job通过
- compare_fp.py正确提取float_spice .meas输出
- 跨格式.meas比较: gain_max差异<2%

## 预估: 45min, 依赖: P5.4.3 (.meas求值)
