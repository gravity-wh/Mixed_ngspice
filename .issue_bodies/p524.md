## 交付物
ac_solve() 函数 + main()中的AC输出调度, 端到端频率扫描。

## 实现路径
1. ac_solve() (~line 1444后):
   - 对数间隔频率点: f_i = fstart * 10^(i/ndec)
   - 每频率: ac_assemble_matrix → lu_solve_complex → 存储v_mag, v_phase
   - 输出表: Freq(Hz) V(node1) V(node2)...

2. .control print vdb/vp处理:
   - vdb(node) → 20*log10f(v_mag[node])
   - vp(node) → atan2f(xi[node], xr[node])*180/PI

3. main() (~line 1660): if(do_ac) { dc_solve; ac_solve; }

## 测试标准
- 04_ota_ac: 输出频率表无NaN
- RC低通f=159Hz: |Vout|=0.707*Vin
- vdb(out)@DC = 20*log10(DC增益)
- 05_opamp_ac: vdb形状 DC平坦 → 高频-20dB/dec
- cvtss2sd <= 5

## 预估: 1h, 依赖: P5.2.1+P5.2.3
