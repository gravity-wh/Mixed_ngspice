## 交付物
.noise解析 + main()调度 + 噪声输出表格式。

## 实现路径
1. Circuit加: do_noise, noise_output[32], noise_input[32], noise_type, noise_n, noise_fstart, noise_fstop
2. parse_netlist: .noise v(out) vinn dec 10 1 10G → sscanf
3. .control块: 同样解析 noise 关键字
4. main(): if(do_noise){dc_solve; noise_solve;}
5. 输出格式: "Freq(Hz)  onoise(V/rtHz)" 两列表

## 测试标准
- 04_ota_noise: 不崩溃, 输出101个频率点
- onoise@1Hz > onoise@10GHz (1/f形状)
- 所有值有限且>0
- cvtss2sd <= 5

## 预估: 45min, 依赖: P5.3.2
