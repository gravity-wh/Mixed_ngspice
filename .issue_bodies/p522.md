## 交付物
Vsource结构体添加 ac_mag/ac_phase 字段 + `.ac dec N fstart fstop` 行解析 + `.control ac` 解析。

## 实现路径
1. Vsource结构体(~line 52): 加 `REAL ac_mag; REAL ac_phase;`
2. Circuit结构体(~line 88): 加 `int do_ac, ac_type; REAL ac_fstart, ac_fstop; int ac_n;`
3. parse_instance() V段(~line 700): DC值后查找 `AC` 关键字, strtof取magnitude和phase
4. parse_netlist()(~line 888): `.ac dec 10 1k 10G` → sscanf type, n, fstart, fstop
5. .control块(~line 920): 同样解析 `ac` 关键字

## 测试标准
- 解析 04_ota_ac: do_ac=1, ac_type=0, ac_n=10, ac_fstart=1e3, ac_fstop=1e10
- VINP ac_mag=1.0; VINN ac_mag=0.0
- cvtss2sd <= 5

## 预估: 1h, 无依赖
