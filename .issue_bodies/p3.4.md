## 目标
解析 `.ic v(n1)=1.0 v(n2)=0.5` 并在 dc_solve/tran_solve 中注入初始电压。
## 解析: find_or_add_node + 存储 ic_nodes[]/ic_vals[]
## dc_solve: v[] 初始化时用 ic_vals 作为猜测值
## tran_solve: .tran uic → 跳过 DC OP, 直接用 ic 值
## 验证: 08_roessler .ic v(x)=1 v(y)=1 v(z)=1 → nic=3
## 预估: 30min | 依赖: 无
