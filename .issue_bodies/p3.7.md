## 目标
.control 块中 `let gm = @m1[gm]` 正确解析 + DC OP 后求值。
## 当前状态: 框架已在 v2.5 代码中 (eval_expr, emit_device_param_table, re_eval_lets), 需验证 P0.1 修复后正常工作。
## 验证: 01_nmos_dc let gm=@m1[gm] → gm 值 ≠ ids
## 预估: 30min | 依赖: P0.1
