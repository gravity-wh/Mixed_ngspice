## 交付物
.ic v(n1)=1.0 v(n2)=0.5 解析 + 在dc_solve/tran_solve中注入初始电压。

## 实现路径
1. Circuit加: int nic; int ic_nodes[64]; REAL ic_vals[64];
2. parse_netlist: .ic v(x)=1.0 v(y)=1.0 v(z)=1.0 → find_or_add_node + 存储
3. dc_solve(~line 1095): v[]初始化后, 对每个ic_node设置v[node]=ic_val (作为初始猜测, 非约束)
4. tran_solve(~line 1470): .tran uic → 跳过DC OP, v[]直接用.ic值

## 测试标准
- 08_roessler: nic=3, nodes={x,y,z}, vals={1.0,1.0,1.0}
- dc_solve: v[ic_node] = ic_val 在第一次牛顿迭代前
- tran uic: 不使用DC OP
- cvtss2sd <= 5

## 预估: 30min, 无依赖
