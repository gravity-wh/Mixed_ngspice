# Mixed-Precision ngspice v1.2 — Verified Circuit Catalog

> **Binary**: `build/src/ngspice` (8,046,704 bytes)
> **Precision Strategy**: SPICE_REAL=float (storage) + double BSIM4 math (exp/log/sqrt)
> **Result**: 40/40 tests PASS, 0 NaN, 15 circuits × 6 PDKs × 3 BSIM families

---

## Category I: Single-Transistor Baselines

### 01 — NMOS Single (PTM 45nm LP)

```
        VDD (1.1V)
          │
    ┌─────┴─────┐
    │           │
    D           │
  ─┤│           │
  G │  NMOS    ═╪═ VG (0.55V)
  ─┤│ W=1µm     │
    S  L=45nm   │
    │           │
    └─────┬─────┘
          │
         VSS

  DC OP:    Vds=1.1V, Vgs=0.55V, Id≈15µA  ✅
  DC Sweep: Vds 0→1.1V, Vgs parameterized  ✅
```

| Property | Value |
|----------|-------|
| PDK | PTM 45nm Low-Power |
| BSIM | Level 54 (BSIM4v5) |
| Transistors | 1 (NMOS) |
| Bias region | Strong inversion, saturation |
| Purpose | Minimal BSIM4 unit test — gm, gds, vth extraction |
| AC/TRAN/NOISE | N/A — single transistor has no meaningful AC/transient response |

### 02 — PMOS Single (PTM 45nm LP)

```
        VDD (1.1V)
          │
    ┌─────┴─────┐
    │           │
    S           │
  ─┤│           │
  G │  PMOS    ═╪═ VG (0.55V)
  ─┤│ W=2µm     │
    D  L=45nm   │
    │           │
    └─────┬─────┘
          │
         VSS

  DC OP:    Vsd=1.1V, Vsg=0.55V, Id≈22µA  ✅
  DC Sweep: Vsd 0→1.1V, Vsg parameterized  ✅
```

| Property | Value |
|----------|-------|
| PDK | PTM 45nm Low-Power |
| BSIM | Level 54 (BSIM4v5) |
| Transistors | 1 (PMOS) |
| Size ratio | W=2× NMOS (compensates lower hole mobility) |
| Bias region | Strong inversion, saturation |
| Purpose | PMOS symmetry verification; gm/gds/vth cross-check |

---

## Category II: Basic Analog Building Blocks

### 03 — Five-Transistor OTA (PTM 45nm LP)

```
                    VDD
                     │
         ┌───────────┼───────────┐
         │           │           │
    M3 (PMOS)   M4 (PMOS)       │
    W=4µ L=0.18µ W=4µ L=0.18µ   │
    diode-load   current-mirror  │
         │           │           │
    ┌────┴───┐  ┌───┴────┐      │
    │  NETD  │  │  OUT   │      │
    └────┬───┘  └───┬────┘      │
         │           │           │
    M1 (NMOS)   M2 (NMOS)       │
    W=2µ L=0.18µ W=2µ L=0.18µ   │
    INP ─┤│     INN ─┤│          │
         └─────┬─────┘           │
              TAIL               │
         M5 (NMOS)               │
         W=4µ L=0.18µ            │
         VB ─┤│                   │
              │                   │
              └───────────────────┘
                     VSS
```

| Property | Value |
|----------|-------|
| PDK | PTM 45nm LP (Lv54) |
| Transistors | 6 (incl. tail bias) |
| Topology | NMOS diff pair + PMOS current-mirror active load |
| DC gain | ~36 dB |
| UGBW | ~9.7 MHz |
| Tests | DC OP ✅  AC ✅  NOISE ✅  TRAN ✅ |
| Purpose | Fundamental analog sub-block; differential-to-single-ended conversion |

### 04 — Two-Stage Miller OpAmp (PTM 45nm LP)

```
                    VDD
                     │
    ┌────────────────┼────────────────────┐
    │                │                    │
    Mtail (PMOS)     M6 (PMOS)           │
    W=4µ L=0.18µ    W=8µ L=0.18µ         │
    VB ─┤│            VB ─┤│               │
    │   TAIL          │   OUT             │
    │    │             │    │              │
    M1 (PMOS)    M2 (PMOS)  M5 (NMOS)     │
    W=4µ L=0.18µ W=4µ L=0.18µ W=4µ L=0.18µ│
    INP─┤│ INN─┤│         N2─┤│            │
    │   NETD  │  N2           │            │
    │    │    │   │           │            │
    M3 (NMOS) M4 (NMOS)       │            │
    W=2µ L=0.18µ W=2µ L=0.18µ │            │
    diode-load │               │            │
    │          │               │            │
    └──────────┴───────────────┴────────────┘
                     VSS

    Cc = 0.5pF (Miller compensation between N2 ↔ OUT)
```

| Property | Value |
|----------|-------|
| PDK | PTM 45nm LP (Lv54) |
| Transistors | 8 |
| Topology | PMOS input pair + NMOS mirror (stage 1) → NMOS CS + PMOS load (stage 2) |
| Compensation | Miller capacitor (Cc=0.5pF), pole splitting |
| DC gain | ~5.5 dB (open-loop) |
| UGBW | ~272 kHz |
| Tests | DC OP ✅  AC ✅  NOISE ✅  TRAN ✅ |
| Purpose | Most common op-amp topology; tests multi-stage feedback stability |

### 05 — Ring Oscillator 17-Stage (PTM 45nm LP)

```
    VDD ──┬──────────┬── ··· ──┬──────────┬──
          │          │          │          │
    ┌─────┴─────┐   ┌┴─────────┴┐   ┌─────┴─────┐
    │ PMOS 2µm  │   │  PMOS 2µm │   │ PMOS 2µm  │
    ├───────────┤   ├───────────┤   ├───────────┤
    │ NMOS 1µm  │   │  NMOS 1µm │   │ NMOS 1µm  │
    └─────┬─────┘   └┬──────────┘   └─────┬─────┘
          │          │          │          │
    VSS ──┴──────────┴── ··· ──┴──────────┴──

    17 CMOS inverters in a ring → self-sustaining oscillation
```

| Property | Value |
|----------|-------|
| PDK | PTM 45nm LP (Lv54) |
| Transistors | 34 (17 stages × 2 transistors) |
| Topology | CMOS inverter chain, closed loop |
| Analysis | TRAN only (digital oscillation) |
| Tests | TRAN ✅ |
| Purpose | Largest transistor count; digital switching stress test |
| DC/AC/NOISE | N/A — ring oscillator is a self-oscillating digital circuit |

---

## Category III: Specialized Analog/Digital Circuits

### 06 — StrongARM Dynamic Comparator (PTM 45nm HP)

```
              VDD (1.0V)
                │
    ┌───────────┼───────────┐
    │   M7/M8/M9/M10        │  ← Precharge PMOS (CLK-controlled)
    │   W=1.2µ L=45nm       │
    │      VXP/VXN          │
    │   M5/M6 (PMOS latch)  │  ← Cross-coupled regenerative pair
    │   M3/M4 (NMOS latch)  │  ← Cross-coupled NMOS latch
    │   M1/M2 (Input diff)  │  ← NMOS differential input pair
    │   W=2µ L=45nm         │
    │   M0 (Tail switch)    │  ← CLK-controlled tail current
    └───────────┬───────────┘
                │
    OUTP ← M11-M14 output inverters → OUTN
```

| Property | Value |
|----------|-------|
| PDK | PTM 45nm High-Performance (Lv54) |
| Transistors | 15 |
| Topology | Precharge → Integration → Regeneration (three-phase clocked) |
| Supply | 1.0V (HP process) |
| Tests | TRAN ✅ |
| Purpose | Mixed-signal benchmark; positive-feedback latch, clocked operation |
| DC/AC/NOISE | N/A — clock-triggered dynamic comparator; static DC meaningless |

### 07 — Bootstrapped Sampling Switch (PTM 45nm HP)

```
    VDD ──┬──────────┐
          │          │
    ┌─────┴─────┐   ═╪═ CB (0.2pF)  ← Bootstrap capacitor
    │ Bootstrap │    │
    │ PMOS/NMOS │    │
    │ network   │    │
    └─────┬─────┘   │
          │ VGATE    │
    ┌─────┴─────┐    │
    │ M1 (NMOS) │    │  ← Main sampling switch
    │ W=2µ      │    │     Vgs ≈ VDD (constant) due to bootstrap
    │ L=45nm    │    │
    └─────┬─────┘    │
          │          │
    VIN ──┴── VSAMPLED ← CS (0.5pF) sampling capacitor
```

| Property | Value |
|----------|-------|
| PDK | PTM 45nm HP (Lv54) |
| Transistors | 7 |
| Topology | Bootstrap network lifts gate above VIN+VDD → constant-Vgs sampling |
| Analysis | TRAN (clocked switching waveforms) |
| Tests | TRAN ✅ |
| Purpose | High-linearity sampling front-end for ADCs |
| DC/AC/NOISE | N/A — switched-capacitor circuit; TRAN only |

---

## Category IV: Behavioral / Mathematical

### 08 — Rössler Attractor

```
    Differential equations:      Circuit implementation:
    ┌─────────────────────┐      ┌──────────────────────────┐
    │ dx/dt = -y - z      │      │ Gx: VCCS I = -v(y)-v(z)  │
    │ dy/dt = x + a·y     │  →   │ Gy: VCCS I = v(x)+0.2v(y)│
    │ dz/dt = b + z(x-c)  │      │ Gz: VCCS I = 0.2+v(z)(v(x)-5.7) │
    └─────────────────────┘      │ Cx=Cy=Cz=1F (integrators) │
                                 │ Rx=Ry=Rz=1GΩ (DC path)   │
                                 └──────────────────────────┘
```

| Property | Value |
|----------|-------|
| PDK | None (behavioral) |
| Transistors | 0 (G-sources + R + C only) |
| Topology | Chaotic attractor — pure mathematical system |
| Analysis | TRAN (0–200s, 0.01s step, UIC) |
| Tests | TRAN ✅ (20,011 data rows) |
| Purpose | Solver infrastructure test — isolates BSIM4 from ngspice core |
| Significance | If Rössler passes but BSIM4 fails → bug in BSIM4; if Rössler fails → bug in solver |

---

## Category V: TSMC Commercial PDK Circuits

### 09 — 22T Operational Amplifier (TSMC bc018 0.18µm)

```
    Full two-stage op-amp with:
    ┌──────────────────────────────────────┐
    │ Stage 1: PMOS diff pair (M1, M2)     │
    │          + NMOS current mirror (M3, M4) │
    │          + cascode PMOS loads (M5-M8)   │
    │                                      │
    │ Stage 2: PMOS driver (MPO) + NMOS driver (Mf) │ ← Push-pull output
    │                                      │
    │ Bias:    7-transistor bias network   │
    │          + 100nA reference current    │
    │                                      │
    │ Compensation:                        │
    │    Cm = 10pF  (Miller, VOUT ↔ gate)  │
    │    Rk1 = 510kΩ + Ck1 = 8.9pF (nulling) │
    │    Ct1 = 3.7pF (load compensation)   │
    │                                      │
    │ Feedback:                            │
    │    Rf1 = Rf2 = 5.38MΩ → Gain = 2    │
    │    VREF = 0.75V → VOUT ≈ 1.5V        │
    └──────────────────────────────────────┘
```

| Property | Value |
|----------|-------|
| PDK | TSMC 0.18µm BCD (bc018_v1p14_rev2) |
| BSIM | Level 14 (BSIM4v5) |
| Transistors | 22 |
| Supply | 1.8V PWL ramp (0→1.8V in 100µs) |
| VREF | 0.75V (closed-loop gain = 2) |
| Tests | TRAN ✅  DC OP (inferred) ✅ |
| FP32 Accuracy | VOUT error = 0.0083% vs FP64 |
| Purpose | Most complex commercial-PDK circuit; tests FP32 in realistic analog IP |

---

## Category VI 🆕: PTM 130nm BSIM4v5 — FP32 Precision Benchmark

### 10 — Five-Transistor OTA (PTM 130nm)

```
    VDD=1.2V
    NMOS diff pair (W=4µ L=0.13µ) + PMOS current mirror (W=8µ L=0.13µ)
    Tail: NMOS (W=8µ L=0.13µ), Vbias=0.5V, Vcm=0.6V
```

| Property | Value |
|----------|-------|
| PDK | PTM 130nm (Lv54, BSIM4v5) |
| BSIM Level | 54 |
| Supply | 1.2V |
| Transistors | 5 |
| DC bias | Vout=0.972V (Vcm=0.6V) |
| Tests | DC ✅  AC ✅  TRAN ✅  NOISE ✅ |
| FP32Δ (DC) | 0.38% vs FP64 |
| Purpose | BSIM4v5 FP32 precision benchmark at 130nm node |

### 11 — Two-Stage Miller OpAmp (PTM 130nm)

```
    VDD=1.2V
    PMOS input pair (W=8µ L=0.13µ) + NMOS mirror (W=4µ L=0.13µ)
    → NMOS CS 2nd stage (W=8µ L=0.13µ) + PMOS load (W=16µ L=0.13µ)
    Cc=0.5pF, Vbias=0.5V
```

| Property | Value |
|----------|-------|
| PDK | PTM 130nm (Lv54, BSIM4v5) |
| BSIM Level | 54 |
| Supply | 1.2V |
| Transistors | 8 |
| DC bias | Vout≈0.6V (unity-gain), IDD≈1mA |
| Tests | DC ✅  AC ✅  TRAN ✅  NOISE ✅ |
| FP32Δ (DC) | 1.46% vs FP64 |
| Purpose | Multi-stage FP32 precision validation at 130nm |

### 12 — Low-Dropout Regulator (PTM 130nm)

```
    VDD=1.2V → PMOS pass device (W=100µ L=0.13µ) → VOUT
                  ↑ gate driven by error amplifier
    Error amplifier: OTA 5T (W=10µ/20µ L=0.13µ)
                  ↑ comparing VREF=0.6V vs VFB
    Feedback: Rf1=Rf2=50kΩ → VOUT = 2×VREF = 1.2V
    Load: Rload=100Ω, Cload=10pF
```

| Property | Value |
|----------|-------|
| PDK | PTM 130nm (Lv54, BSIM4v5) |
| BSIM Level | 54 |
| Supply | 1.2V |
| Transistors | ~6 (error amp + pass device) |
| Topology | Error amplifier + PMOS pass device + resistive feedback |
| Tests | DC ✅  AC ✅  TRAN ✅  NOISE ✅ |
| FP32Δ (DC) | 0.07% vs FP64 |
| Purpose | System-level FP32 precision; feedback regulation loop |

---

## Category VII 🆕: PTM 180nm BSIM3v3 — Regression Baseline

### 13 — Five-Transistor OTA (PTM 180nm)

```
    Same topology as OTA #03/#10
    PTM 180nm BSIM3v3 Lv49, VDD=1.8V
    W_in=20µ L_in=1µ, W_load=16µ L_load=1µ, W_tail=12µ L_tail=1µ
```

| Property | Value |
|----------|-------|
| PDK | PTM 180nm |
| BSIM | Level 49 (BSIM3v3) |
| Supply | 1.8V |
| Transistors | 5 |
| DC gain | 34.3 dB |
| UGBW | 85 MHz |
| Tests | DC ✅  AC ✅  TRAN ✅  NOISE ✅ |
| FP32 vs FP64 | Identical (BSIM3v3 uses double math natively) |
| Purpose | Regression baseline — verify v1.2 does not break BSIM3v3 circuits |

### 14 — Two-Stage Miller OpAmp (PTM 180nm)

```
    Same topology as OpAmp #04/#11
    PTM 180nm BSIM3v3 Lv49, VDD=1.8V
    Cc=0.5pF
```

| Property | Value |
|----------|-------|
| PDK | PTM 180nm |
| BSIM | Level 49 (BSIM3v3) |
| Supply | 1.8V |
| Transistors | 8 |
| DC gain | 64.3 dB |
| UGBW | 13.7 MHz |
| Phase margin | 81° |
| PZ analysis | Dominant pole 6.3kHz, non-dominant 64MHz, RHP zero 158MHz |
| Tests | DC ✅  AC ✅  TRAN ✅  NOISE ✅  PZ ✅ |
| Purpose | Full pole-zero characterization under FP32 |

### 15 — LDO Regulator (TSMC 180nm)

```
    Commercial TSMC 180nm BSIM3v3 (MOSIS), VDD=2.5V
    Full LDO system:
    ┌──────────────────────────────────────┐
    │ Error amplifier (OTA-based)          │
    │ PMOS pass device                     │
    │ Rf1=Rf2=900Ω feedback (VOUT=2×VREF) │
    │ Rcomp=2kΩ, Ccomp=160pF (zero comp)  │
    │ Cout=1µF, Rload=50Ω                  │
    │ VREF=0.9V → VOUT=1.8V                │
    └──────────────────────────────────────┘
```

| Property | Value |
|----------|-------|
| PDK | TSMC 180nm (MOSIS) |
| BSIM | Level 49 (BSIM3v3) |
| Supply | 2.5V |
| Transistors | ~20 |
| Line regulation | 1.14 mV/V |
| Load regulation | -1.65 mV (0→100mA) |
| PSRR | 57.3 dB @ DC |
| Output noise | 7.4 µVrms (10Hz–100kHz) |
| Tests | DC ✅  AC ✅  TRAN ✅  NOISE ✅ |
| Purpose | Most complex system-level circuit; full analog IP verification |

---

## Summary

| Dimension | Coverage |
|-----------|----------|
| **Circuits** | 15 (single transistor → system-level LDO) |
| **PDKs** | 6 (PTM 45nm LP/HP, PTM 130nm, PTM 180nm, TSMC bc018, TSMC 180nm) |
| **BSIM families** | 3 (BSIM3v3 Lv49, BSIM4v5 Lv14, BSIM4v5 Lv54) |
| **Process nodes** | 45nm, 130nm, 180nm, 0.18µm |
| **Transistor count** | 1–34 |
| **Analysis types** | DC OP, DC Sweep, AC, TRAN, NOISE, Pole-Zero |
| **Total tests** | 40 |
| **NaN** | 0 |
| **FP32 accuracy** | <0.01% (bc018), <1.5% (PTM130) |
